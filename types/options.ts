export interface PatientOption {
  id: string;
  first_name: string;
  last_name: string;
}

export interface DentistOption {
  id: string;
  full_name: string;
}

export interface StatusOption {
  value: string;
  label: string;
}